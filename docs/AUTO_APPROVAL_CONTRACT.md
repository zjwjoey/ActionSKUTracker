# Auto Approval Contract

V1 is shadow-only by default. Only cat1, cat2 and simple spec may be considered; name, description and details require human review. Rules, source freshness, conflict gates, validator PASS and number preservation are mandatory; confidence alone is insufficient. Human decisions always win and AI never auto-promotes dictionary rules.
