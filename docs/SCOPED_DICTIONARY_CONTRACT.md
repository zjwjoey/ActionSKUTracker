# Scoped Dictionary Contract

Rules match current Spanish `cat1_es`/`cat2_es` and are ordered PRODUCT > CAT2+FIELD > CAT2 > CAT1+FIELD > CAT1 > FIELD > GLOBAL. Only HUMAN_APPROVED/APPROVED rules match production resolution. Same-level different values fail closed. Every proposed rule requires a blast-radius preview; AI cannot promote a rule.
