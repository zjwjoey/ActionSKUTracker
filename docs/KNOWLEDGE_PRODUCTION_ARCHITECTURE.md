# Knowledge Production Architecture V1

SQLite PRIMARY Spanish facts are immutable input. Dictionary and scoped rules are knowledge sources. Resolver output is a field-level proposal. Queue/candidates/review are evidence only. Approved values are written, field by field, to `product_localizations`; ES/ZH exports read that formal localization and never AI candidates. Production apply remains disabled by default.
