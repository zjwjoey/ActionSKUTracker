# Knowledge Rollback

Localization apply is transactional and field-level. A stale preview, non-PRIMARY database, disabled switch, unapproved candidate or failed validation blocks the write. Spanish facts, lifecycle, prices, events, images and Master files are outside the write set. Restore uses the existing database backup/commit-batch mechanisms; candidates and queue rows remain audit evidence.
